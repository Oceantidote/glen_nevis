<%= simple_form_for :user, url: path, method: @user.id.nil? ? :post : :patch do |f| %>
    <%= f.input :email %>
    <%= f.input :password, as: :string %>
    <%= f.submit @user.id.nil? ? 'Add User' : 'Update User' %>
<% end %>
